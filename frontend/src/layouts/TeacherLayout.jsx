// Recreated stub — original deleted during Phase 2 cleanup. Was unused.
import React from 'react';
import { Outlet } from 'react-router-dom';

const TeacherLayout = () => <Outlet />;

export default TeacherLayout;
