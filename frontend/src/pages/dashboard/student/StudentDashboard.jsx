// Recreated stub — original was a legacy duplicate of pages/StudentDashboard.jsx.
// Re-exports the active root-level Student dashboard page to preserve legacy path.
export { default } from '../../Student/Dashboard';
